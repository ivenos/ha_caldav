[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
addopts = -m "not live" --strict-markers --strict-config
timeout = 120
markers =
    live: needs a real CalDAV server, see tests/live-test.sh
